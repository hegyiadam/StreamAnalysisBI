<?php
include 'database_reader.php'; 

/*$db = connectToDb();


$ret=pg_query($db, "INSERT INTO opinions (positive, negative,filled) VALUES (0.0,0.0,false) RETURNING transaction_code;");

if(!$ret) {
    array_push($errs,pg_last_error($db));
    exit;
} 
$row = pg_fetch_row($ret);
$tr_id = $row[0];
//$tr_id = str_replace("\"","", $tr_id);

pg_close($db);
echo $tr_id;*/



//$op = array("<a>" . exec('python service.py', $res) . "</a>");
//echo json_encode($opinion);

?>